import 'package:flutter/material.dart';

class FeedOptimizationScreen extends StatelessWidget {
  const FeedOptimizationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("🌿 Feed Optimization")),
      body: const Center(
        child: Text(
          "Feed Optimization features coming soon...",
          style: TextStyle(fontSize: 16),
        ),
      ),
    );
  }
}
